# Canvas LMS API Adapter

A PHP library for interacting with the Canvas LMS API.

## Installation

You can install the package via composer:

```bash
composer require tonylabs/canvas-lms-api-adapter
```